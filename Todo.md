# AutoForge 待办路线图

> 基线日期：2026-08-09。本文只记录当前尚未完成或尚未达到生产验收标准的事项；README 中的目标架构不代表已经实现。完成事项必须同时满足本文的通用完成标准，不能仅以“页面可见”或“能够编译”勾选完成。

## 1. 使用规则

优先级定义：

- `P0`：安全边界或核心执行闭环的前置事项，没有完成就不应宣称平台可生产使用。
- `P1`：完整产品能力，完成后才能达到 AutoForge 的总体目标。
- `P2`：规模化、体验或增强能力，不阻塞最小生产闭环。

建议按以下依赖顺序推进：

```text
架构决策与安全基线
  ├─> 身份、LDAP、用户与权限
  └─> 执行契约与状态机
          └─> Agent 领取、执行、日志与产物
                  └─> Lite/Full 队列和后台工作器
                          ├─> 执行页面与运维闭环
                          └─> 分析与洞察

以上能力稳定后 -> 离线安装、升级、备份恢复与生产验收
```

每个事项的通用完成标准：

- 领域规则、应用用例、API 契约和 UI 在 Lite/Full 间共享，模式差异只存在于组合根和基础设施适配器。
- Lite 不连接或加载 PostgreSQL、NATS、MinIO、Redis；Full 不用缓存或队列代替 PostgreSQL 中的业务事实。
- LDAP 是可选的企业身份适配器；未配置时 Lite 仍只依赖 SQLite 和本地文件，启动过程不得探测目录服务。
- 输入、环境变量、协议消息和数据库映射在边界校验；错误包含稳定机器码和 `requestId`，不暴露堆栈或秘密。
- SQLite/PostgreSQL 分别提供版本化迁移，并验证全新建库、从上一版本升级和失败恢复。
- 覆盖成功、边界、失败、幂等、并发和恢复测试；涉及适配器时运行 Lite/Full 契约测试。
- 页面包含加载、空数据、错误、无权限和离线状态，并满足键盘操作与基本可访问性要求。
- 更新 README、配置示例、架构/运维文档；不把规划能力描述成已完成。
- 运行与变更范围匹配的格式、lint、类型、测试和构建命令，并记录实际结果。

## 2. 当前基线（不重复列入待办）

以下能力已有首版实现，后续待办只记录其缺失部分：

- Next.js 16 主平台、方案 E UI 基线和 Node.js 24 LTS 构建。
- TestNG JAR 有界静态扫描、内容寻址存储、扫描预览和用例导入。
- SQLite/PostgreSQL 用例与批次持久化，本地文件/MinIO 对象存储。
- 用例任务创建及任务内用例新增、删除。
- Runner 注册、身份落盘、资源心跳、在线判定和资源感知初始分配。
- Agent 本地受控单命令执行核心，以及默认关闭的直连 WebSocket 终端。
- 四平台后端镜像与 Agent 二进制的 GitHub Release 构建、SBOM、校验和与构建来源证明。

当前最重要的边界是：Lite/Full 已接通 assignment、lease、Agent 执行、资源限制、日志、产物、完成上报与后台调度首版；仍需完成严格磁盘配额、全量项目/租户隔离、分析和断网发布验收，才能达到生产完成定义。

## 3. M0 — 架构决策与生产安全基线

- [x] **AF-M0-001 `P0` 固化身份与访问控制 ADR。** 明确本地账号和 LDAP 的优先级、登录回退策略、会话模型、账号合并规则、紧急管理员恢复方式、Lite/Full 一致语义和离线限制。
- [x] **AF-M0-002 `P0` 固化授权和资源边界 ADR。** 明确组织/项目作用域、系统角色与项目角色、权限继承、默认拒绝、资源所有权以及未来多租户的数据隔离方式。
- [x] **AF-M0-003 `P0` 发布 Runner Protocol v1 契约。** 固化 assignment、claim、lease、续租、取消、日志、产物、完成和 reconcile 的 schema、错误码、大小限制、幂等键及相邻版本兼容策略。
- [x] **AF-M0-004 `P0` 固化执行状态机。** 为 `RunBatch`、`ExecutionRun`、`RunAttempt`、`Assignment` 和 `Lease` 定义唯一合法迁移，以及取消、超时、完成、迟到结果和租约过期竞争的裁决规则。
- [x] **AF-M0-005 `P0` 固化密文管理 ADR。** 选择离线可用的主密钥来源、信封加密方式、密钥轮换、备份恢复、脱敏和最小暴露方案；禁止在普通环境变量快照、日志和 spool 中保存明文密文。
- [x] **AF-M0-006 `P0` 完成 Runner 执行威胁模型。** 列出受信任边界、process 执行器已知风险、主机加固要求、网络/文件/进程隔离和恶意 JAR、日志、产物、终端的攻击面。
- [x] **AF-M0-007 `P1` 确定项目许可证。** 增加根 `LICENSE`，审查第三方依赖与离线再分发条件，并生成许可证清单；在此之前不得宣称允许再分发或商用。
- [ ] **AF-M0-008 `P1` 补齐目标模块边界。** 随首个真实行为落地 `runner-sdk`、`executors`、`queue`、`cache` 和 `apps/worker`，保持 domain/application/contracts 到基础设施的依赖方向；不得创建空包或占位目录。

## 4. M1 — 登录、LDAP、用户管理与权限分层

### 4.1 本地身份与会话

- [x] **AF-IAM-001 `P0` 建立统一身份领域模型。** 增加 `User`、外部身份、登录会话、角色、权限、成员关系和审计事件模型，使用应用层 UUIDv7 与 UTC 时间。
- [x] **AF-IAM-002 `P0` 完成双数据库身份迁移。** SQLite/PostgreSQL 同时支持用户、凭据、外部身份、会话、角色、授权关系和安全事件，并提供唯一性、版本条件和禁用约束。
- [x] **AF-IAM-003 `P0` 实现首位管理员引导。** 首次离线启动可通过一次性引导凭据创建本地系统管理员；凭据使用后失效，不能依赖邮件或公网服务。
- [x] **AF-IAM-004 `P0` 实现本地账号登录。** 使用经过评审的密码哈希、恒定时间校验、安全 Cookie、会话过期/轮换、登出和全会话撤销；密码和会话令牌不进入日志。
- [x] **AF-IAM-005 `P0` 实现离线密码恢复。** 管理员可重置本地账号、强制下次修改、解锁账号并撤销已有会话，不把邮件服务作为恢复前提。
- [x] **AF-IAM-006 `P0` 加固认证入口。** 提供 CSRF 防护、登录限速、失败锁定/退避、会话固定攻击防护、安全响应头和通用失败提示，并保留可审计原因。
- [x] **AF-IAM-007 `P1` 实现会话管理。** 用户查看并终止自己的会话，管理员按用户撤销会话；敏感权限变化后强制刷新或终止旧会话。

### 4.2 LDAP 配置与同步

- [x] **AF-LDAP-001 `P0` 定义 LDAP 配置 schema。** 覆盖服务器列表、连接/读取超时、LDAP over TLS/StartTLS、私有 CA、bind DN、加密 bind 密码、Base DN、用户/组过滤器、属性映射和分页上限。
- [x] **AF-LDAP-002 `P0` 实现 LDAP 配置管理。** 在系统设置中提供读取、修改、启停和连接测试；返回脱敏且可操作的 DNS、TLS、绑定、过滤器和权限诊断，不回显 bind 密码。
- [x] **AF-LDAP-003 `P0` 支持离线内网 TLS。** 可导入受控私有 CA，严格校验主机名和证书有效期；禁止默认跳过证书校验，CA 文件进入备份和轮换流程。
- [x] **AF-LDAP-004 `P0` 实现 LDAP 登录。** 用稳定外部标识关联本地 `User`，限制搜索结果和响应时间，防止 LDAP filter 注入，并清晰区分凭据错误、目录不可用和账号策略拒绝。
- [x] **AF-LDAP-005 `P0` 实现 LDAP 即时建号。** 首次成功登录按映射创建或更新用户，定义用户名/邮箱冲突、属性变更和本地账号合并规则，禁止仅凭可变邮箱静默接管账号。
- [x] **AF-LDAP-006 `P0` 实现 LDAP 组到角色映射。** 支持多个组、显式优先级、作用域和默认最小角色；未匹配用户默认拒绝或赋予明确的最小权限，不得隐式成为管理员。
- [ ] **AF-LDAP-007 `P1` 实现手动与计划同步。** 同步用户、组和状态，支持游标/分页、有界并发、断点、结果摘要和幂等重跑；Lite 使用 SQLite 持久任务，Full 使用统一后台任务端口。
- [x] **AF-LDAP-008 `P0` 定义目录失联策略。** 明确已有会话、新登录、本地紧急管理员、同步滞后和恢复后的行为；不得因 LDAP 暂时不可用锁死所有管理入口。
- [x] **AF-LDAP-009 `P0` 实现停用与离职处理。** 目录账号或组资格被移除后，按策略禁用访问、撤销会话和高风险终端权限，同时保留历史执行记录归属。
- [ ] **AF-LDAP-010 `P1` 增加 LDAP 测试矩阵。** 覆盖 TLS/StartTLS、私有 CA、分页、嵌套/多组映射、特殊字符、超时、目录失联、重复身份、停用和 Lite/Full 行为一致性。

### 4.3 用户管理与权限分层

- [x] **AF-IAM-008 `P0` 实现用户管理 API 与页面。** 支持游标分页、搜索、来源筛选、创建本地用户、查看详情、启用/禁用、解锁、重置凭据和撤销会话；LDAP 管理的属性明确只读。
- [x] **AF-IAM-009 `P0` 定义稳定权限目录。** 至少覆盖用例/来源、用例任务、批次/执行、日志/产物、Runner、直连终端、环境/密文、用户、角色、LDAP、审计和系统设置的 `read/create/update/delete/execute/admin` 动作。
- [x] **AF-IAM-010 `P0` 提供内置分层角色。** 至少定义系统管理员、项目管理员、测试管理员、执行操作员、只读观察者和审计员，并记录每个角色的精确权限；终端访问不得包含在普通执行权限中。
- [ ] **AF-IAM-011 `P1` 支持自定义角色。** 角色可复制、编辑和停用；禁止删除仍被引用的角色或移除系统最后一位管理员，权限变更全量审计。
- [x] **AF-IAM-012 `P0` 实现服务端授权。** Route Handler、Server Action、Server Component、对象下载和 WebSocket 握手都调用共享授权用例；隐藏按钮只能改善体验，不能成为安全边界。
- [x] **AF-IAM-013 `P0` 实现数据作用域过滤。** 列表、详情、全局搜索、日志、产物和分析查询只返回当前身份有权访问的组织/项目数据，禁止通过猜测 ID 越权。
- [x] **AF-IAM-014 `P0` 将直连终端纳入 RBAC。** 以登录会话和短时一次性票据代替独立管理员访问令牌作为正常入口，保留 Runner 身份校验、会话时限、并发限制和完整审计。
- [ ] **AF-IAM-015 `P1` 实现项目与成员管理。** 支持创建/归档项目、添加/移除成员、分配项目角色和转移负责人；历史数据不可因成员删除失去归属。
- [ ] **AF-IAM-016 `P1` 完成多租户隔离。** 为所有业务聚合、仓储查询、唯一索引、对象键和缓存键加入明确租户/项目作用域，并用跨租户越权测试验证。
- [ ] **AF-IAM-017 `P1` 实现 API 令牌/服务账号。** 令牌只显示一次、存储摘要、具有作用域和过期时间，可撤销轮换；Runner 身份、用户会话和自动化 API 身份保持分离。
- [x] **AF-IAM-018 `P0` 建立权限矩阵测试。** 对每个内置角色验证页面、API、对象下载和 WebSocket 的允许/拒绝结果，覆盖水平越权、禁用用户、过期会话和权限实时变更。

### 4.4 审计

- [x] **AF-AUDIT-001 `P0` 建立持久审计事件。** 记录登录、LDAP 配置/同步、用户与角色变更、用例来源变更、批次操作、Runner 管理、终端会话、密文访问、取消和重试；只记录脱敏摘要。
- [x] **AF-AUDIT-002 `P1` 实现审计查询与导出。** 支持按操作者、动作、资源、结果和 UTC 时间过滤，使用游标分页和有界导出，并受独立审计权限控制。
- [x] **AF-AUDIT-003 `P1` 定义审计保留与完整性策略。** 明确保留期、归档、备份、删除授权和篡改检测边界，任何清理都产生新的审计记录。

## 5. M2 — 执行领域、Assignment 与 Lease

- [x] **AF-EXEC-001 `P0` 增加版本化执行契约。** 在 `packages/contracts` 实现 `ExecutionSpec`、assignment、lease、日志、产物、完成、取消和 reconcile schema，并限制字段、条目数和载荷大小。
- [x] **AF-EXEC-002 `P0` 完成执行领域模型。** 为批次、单次执行、尝试、分配和租约实现不变量、版本条件、终态保护和可审计状态历史。
- [x] **AF-EXEC-003 `P0` 增加双数据库执行表与仓储。** SQLite/PostgreSQL 保存 assignment、lease、状态事件、完成确认、日志水位和产物元数据，仓储使用相同契约测试。
- [x] **AF-EXEC-004 `P0` 实现原子 claim。** Agent 通过有界长轮询只领取与自身标签、能力和容量匹配的 assignment；重复 claim 返回同一已知结果，不重复启动 attempt。
- [x] **AF-EXEC-005 `P0` 实现 lease 创建和续租。** lease 使用期限、令牌和版本条件，续租返回取消/排空指令；心跳不能代替 lease。
- [x] **AF-EXEC-006 `P0` 实现 lease 回收与 reconcile。** 后台回收真正过期的租约，处理 Agent 重启、网络分区、迟到完成和新 attempt 竞争，不覆盖已经确认的终态。
- [x] **AF-EXEC-007 `P0` 实现幂等完成上报。** 先持久化结果再向 Agent 确认；相同结果重复提交返回原确认，冲突结果产生稳定错误和审计事件。
- [x] **AF-EXEC-008 `P0` 接通失败重跑。** 根据已固化的 `retryLimit` 在失败/超时后创建下一 attempt，达到上限后形成唯一最终失败；取消和策略性拒绝不被错误重试。
- [x] **AF-EXEC-009 `P0` 实现批次取消和单次取消。** queued、assigned、running 状态都有明确行为，Runner 收到取消后清理进程树，完成/取消竞争只产生一个终态。
- [x] **AF-EXEC-010 `P0` 实现执行超时。** 区分排队超时、领取超时、执行超时和上传收尾超时，所有定时判断使用服务端 UTC 时间并可从崩溃恢复。
- [x] **AF-EXEC-011 `P0` 聚合批次结果。** 从权威 `ExecutionRun`/`RunAttempt` 计算进度、成功、失败、超时、取消和阻塞数量，重复消息不能重复计数。
- [ ] **AF-EXEC-012 `P1` 增加优先级、公平性和配额。** 支持批次优先级、项目并发上限、Runner 并发上限和防饥饿策略，并验证并发调度不超卖。
- [x] **AF-EXEC-013 `P1` 增加 Runner 标签与能力匹配。** 执行快照声明 OS、架构、Java/TestNG/toolchain 等要求，调度器返回可解释的不匹配原因。
- [ ] **AF-EXEC-014 `P1` 增加 Runner 排空、禁用和注销。** 排空停止领取新任务但允许在途任务结束；禁用、凭据撤销和注销不能遗留永久 running 状态。
- [ ] **AF-EXEC-015 `P1` 实现单用例执行。** 与批跑共享 `ExecutionRun`、策略、权限、Runner 匹配、日志和产物闭环，不建立第二套执行路径。
- [x] **AF-EXEC-016 `P0` 实现控制面 Runner SDK。** 在 `packages/runner-sdk` 集中处理协议校验、Runner 认证、重试和兼容协商，区分传输错误与业务拒绝，不在页面或 Route Handler 中复制协议逻辑。

## 6. M3 — Runner Agent 实际执行闭环

### 6.1 领取、执行与恢复

- [x] **AF-AGENT-001 `P0` 实现 Agent claim loop。** 有界长轮询、指数退避与抖动、并发槽位控制、优雅关闭和服务端限流均可配置，空闲时不产生高频请求。
- [x] **AF-AGENT-002 `P0` 实现 lease 续租循环。** 与心跳生命周期分离，正确响应取消/排空/拒绝续租；失去执行权后不得继续普通上报。
- [x] **AF-AGENT-003 `P0` 实现输入获取。** 通过控制面授权下载权威 JAR 和其他输入，校验大小、SHA-256、目标相对路径和可用空间，不让 Agent 持有对象存储长期凭据。
- [x] **AF-AGENT-004 `P0` 实现 TestNG 执行规格。** 将固化的类、方法 JVM descriptor、参数和环境快照转换为明确的 Java 参数数组，不拼接 Shell；定义依赖 JAR/classpath 和退出码语义。
- [x] **AF-AGENT-005 `P0` 定义离线 Java/TestNG 工具链。** Runner capability 上报精确版本，运行时不下载 JDK、TestNG、浏览器、驱动或依赖；不满足要求时在调度前拒绝。
- [x] **AF-AGENT-006 `P0` 接入受控 process supervisor。** 每次 attempt 创建新工作目录，执行文件白名单、本地策略、cwd、环境、超时、日志和清理限制只能被本机策略收紧。
- [x] **AF-AGENT-007 `P0` 补齐资源限制。** 在目标 Linux 平台限制 CPU、内存、磁盘、进程数和文件数量；明确 cgroup/ulimit 等实际机制、权限要求和无法强制时的诊断。
- [x] **AF-AGENT-008 `P0` 实现取消和进程树清理。** 先温和终止、短暂 grace period 后强制清理整个进程组，覆盖超时、服务关闭和 lease 被拒绝场景。
- [x] **AF-AGENT-009 `P0` 实现启动 reconcile。** 扫描本地 attempt/spool 状态并逐项向服务端确认，未经确认不自行重跑，清理仅处理已确认或过期内容。
- [ ] **AF-AGENT-010 `P1` 实现 Runner 凭据轮换与撤销。** 轮换过程可恢复，旧凭据有明确失效点；撤销后终端、心跳、claim 和上报都被拒绝。

### 6.2 日志与结果

- [x] **AF-LOG-001 `P0` 实现 stdout/stderr/agent 分流。** 按字节读取并流式解码 UTF-8，每个块携带 `(attemptId, stream, sequence)`，不因跨块中文或超长行损坏内容。
- [x] **AF-LOG-002 `P0` 实现 Agent 有界 spool。** 日志、结果和上传状态断线落盘，设置磁盘配额、保留期和确认水位；满额时明确截断或终止，不静默丢弃。
- [x] **AF-LOG-003 `P0` 实现日志批量上传与确认。** 控制面按去重键幂等写入，只确认连续序号，Agent 重传缺口后才清理 spool。
- [x] **AF-LOG-004 `P0` 实现双层秘密脱敏。** Agent 写 spool 前和控制面持久化前分别脱敏，覆盖普通环境密文、令牌格式和跨日志块秘密，同时保留明确的替换标记。
- [ ] **AF-LOG-005 `P1` 实现日志查询和保留。** 大日志分页/分段读取，支持 stream、关键字和时间筛选；保留、截断和删除策略与审计、对象清理一致。
- [x] **AF-RESULT-001 `P0` 定义 TestNG 结果映射。** 将进程启动失败、非零退出、断言失败、配置失败、跳过、超时、取消和平台错误映射为稳定结果码与可读摘要。
- [x] **AF-RESULT-002 `P1` 解析结构化 TestNG 报告。** 有界解析 suite/test/class/method 结果和耗时，拒绝实体扩展等不安全 XML 能力，原报告作为产物保留。

### 6.3 产物

- [x] **AF-ART-001 `P0` 实现安全产物发现。** glob 只相对 attempt 目录解析，拒绝绝对路径、`..`、设备文件和越界符号链接，并限制数量、单项/总大小和扫描时间。
- [x] **AF-ART-002 `P0` 实现产物声明与上传。** Agent 先计算 SHA-256；Lite 通过控制面流入本地 ObjectStore，Full 使用短期单对象受控目标，上传完成后服务端复核大小和摘要。
- [x] **AF-ART-003 `P0` 关联产物和执行结果。** 必需产物缺失、可选产物缺失、上传失败和摘要冲突使用不同结果码，重试上传不重复生成元数据。
- [ ] **AF-ART-004 `P1` 实现产物下载、预览与清理。** 鉴权下载使用安全 Content-Disposition/Content-Type，支持报告、截图等受控预览；业务删除通过可重试清理任务处理对象。

### 6.4 Agent 运维与终端

- [x] **AF-AGENT-011 `P1` 提供离线服务安装模板。** 至少包含 systemd 服务、专用低权限用户、目录权限、私有 CA、配置文件、日志轮转、启动/停止和卸载说明。
- [x] **AF-AGENT-012 `P1` 实现版本/能力兼容提示。** 平台展示 Agent、协议、工具链和架构兼容性，阻止不兼容 assignment，并提供离线升级路径。
- [ ] **AF-AGENT-013 `P2` 评估并实现 container 执行器。** 与 process 执行器共享规格和生命周期，显式限制网络、挂载、capability、用户、seccomp、CPU 和内存；只能陈述实际隔离能力，不能把普通容器宣传为完整沙箱。
- [x] **AF-TERM-001 `P0` 完成终端安全整合。** 终端默认关闭，必须通过独立权限、短时票据和 Runner 身份；记录操作者、Runner、开始/结束、断开原因和命令审计边界。
- [ ] **AF-TERM-002 `P1` 完成多 Web 副本终端方案。** 先支持并文档化负载均衡亲和；若需要 NATS 跨实例中继，先完成协议与威胁模型 ADR，且不新增独立 ShellHub 服务。
- [ ] **AF-TERM-003 `P1` 补齐终端故障恢复。** 明确 Agent 重连、浏览器刷新、会话过期、平台重启和进程组清理行为，不允许断开的 Shell 成为孤儿进程。

## 7. M4 — Lite/Full 任务队列、后台工作器与缓存

- [x] **AF-QUEUE-001 `P0` 定义 `JobQueuePort` 契约。** 消息包含稳定 `messageId`、`runId`、`attempt`、`schemaVersion`、创建时间、优先级和去重键，统一视为至少一次投递。
- [x] **AF-QUEUE-002 `P0` 实现 SQLite 持久队列。** 表包含状态、优先级、可用时间、租约所有者/到期、尝试次数、去重键和错误摘要；领取使用原子条件更新。
- [x] **AF-QUEUE-003 `P0` 实现 Lite 嵌入式工作器。** 启动、轮询退避、处理、确认、死信、过期租约回收和有限排空可从崩溃恢复，且不要求外部服务。
- [x] **AF-QUEUE-004 `P0` 实现 JetStream 适配器。** 以版本化代码管理 stream、subject、durable consumer、显式 ack、ack wait、max deliver、保留和死信策略。
- [x] **AF-QUEUE-005 `P0` 实现 PostgreSQL transactional outbox。** 业务状态与待发布事件同事务提交，发布成功后幂等标记，避免数据库提交成功但消息丢失。
- [x] **AF-QUEUE-006 `P0` 实现 Full Dispatcher Worker。** 增加独立 `apps/worker` 组合根，消费调度消息、持久化 assignment 后确认消息；不跨远程执行周期持有队列 ack。
- [x] **AF-QUEUE-007 `P0` 统一 Lite/Full 队列契约测试。** 覆盖重复投递、进程崩溃、过期租约、延迟消息、最大尝试、死信和关闭排空。
- [x] **AF-CACHE-001 `P1` 定义 `CachePort`/限流端口。** 缓存 key 包含命名空间、租户和 schema 版本，cache miss 总能从权威存储恢复。
- [x] **AF-CACHE-002 `P1` 实现 Lite 内存/SQLite 缓存语义。** 进程重启允许丢失并重建，不使用缓存 TTL 决定业务生命周期。
- [x] **AF-CACHE-003 `P1` 实现 Redis 业务适配器。** 只承载可重建缓存、限速和减少竞争的短锁；数据库约束和条件写继续保证关键不变量。
- [x] **AF-WORKER-001 `P1` 完善工作器生命周期。** readiness、并发上限、结构化日志、指标、退避、有限重试、排空和异常退出码均可运维观测。

## 8. M5 — 用例资产、任务和执行配置完善

### 8.1 TestNG 发现与来源生命周期

- [ ] **AF-CASE-001 `P1` 解析 `testng.xml`。** 支持 suite/test 的 include/exclude、参数和 package 选择规则；未支持语义产生有界警告，不能静默声称完整发现。
- [ ] **AF-CASE-002 `P1` 支持可解析范围内的继承测试。** 处理 JAR 内父类的 TestNG 注解继承；JAR 外父类保持明确警告，不加载或执行用户 class。
- [ ] **AF-CASE-003 `P1` 处理 Multi-Release JAR。** 按目标 Java 版本选择 `META-INF/versions` class，或明确拒绝不确定输入，不能继续无声忽略。
- [ ] **AF-CASE-004 `P1` 明确工厂和动态语义。** 为 Factory、DataProvider、监听器和运行时动态测试定义静态发现边界、执行期映射和用户可见警告。
- [ ] **AF-CASE-005 `P1` 实现来源版本对比。** 展示新增、变化、移除和冲突用例，设置权威全量 JAR 时提供预览和确认，不直接破坏历史版本。
- [ ] **AF-CASE-006 `P1` 定义权威来源同步规则。** 明确来源中消失的用例是禁用、归档还是保留，并确保已有任务和历史执行仍引用不可变版本。
- [ ] **AF-CASE-007 `P1` 实现来源删除与对象回收。** 有引用时拒绝或归档，无引用对象通过可重试清理任务删除；本地/MinIO 行为一致。
- [ ] **AF-CASE-008 `P1` 完善大 JAR 导入。** 在现有大小/条目/解压上限基础上支持后台进度、取消、失败诊断和幂等重试，不在 Web 请求中进行无界工作。

### 8.2 用例与任务管理

- [ ] **AF-CASE-009 `P1` 完善用例元数据。** 支持标签、分组、启停、展示名、描述和可编辑定义；任何执行都引用不可变 `CaseVersion` 快照。
- [ ] **AF-CASE-010 `P1` 实现用例版本历史。** 可查看差异、来源、创建人和关联执行，允许从旧版本创建新版本但不覆盖历史记录。
- [ ] **AF-SUITE-001 `P1` 完善用例任务生命周期。** 支持重命名、描述、复制、归档、批量增删、启停和版本/变更历史，并处理并发编辑冲突。
- [ ] **AF-SUITE-002 `P1` 增加任务执行策略。** 配置优先级、并发度、重试、排队/执行超时、Runner 选择器、参数模板和产物规则，并在开始前校验。
- [ ] **AF-SUITE-003 `P2` 支持计划触发。** 在手动执行闭环稳定后增加离线可用的定时计划、时区、错过触发策略、暂停和幂等触发记录。

### 8.3 环境与密文

- [x] **AF-ENV-001 `P0` 建立执行环境模型。** 将当前批次明文环境变量升级为可复用、可版本化的环境配置，并在执行时固化非密文快照。
- [x] **AF-ENV-002 `P0` 建立密文引用。** 密文加密存储、按项目授权、只显示元数据，Agent 仅在获得有效 lease 后按需接收并注入内存/子进程环境。
- [x] **AF-ENV-003 `P0` 实现执行前校验。** 校验必需参数、变量名、密文可用性、Runner capability、JAR/工具链和资源限制，向用户返回逐项阻塞原因。
- [ ] **AF-ENV-004 `P1` 实现环境管理 UI/API。** 支持创建、复制、版本、停用、引用查询和权限控制；敏感值永不回显、导出或进入审计详情。

## 9. M6 — 主平台页面与交互闭环

- [ ] **AF-UI-001 `P0` 实现登录和无权限页面。** 提供本地/LDAP 登录入口、会话过期恢复、禁止访问状态和可访问的错误反馈。
- [ ] **AF-UI-002 `P0` 实现用户、角色、项目和 LDAP 设置页面。** 保持方案 E 视觉语言，高风险修改显示影响范围、二次确认和审计提示。
- [ ] **AF-UI-003 `P0` 实现执行记录列表。** URL 保存项目、任务、用例、状态、Runner 和时间筛选，使用服务端游标分页并显示真实聚合状态。
- [ ] **AF-UI-004 `P0` 实现执行详情。** 包含概览、实时日志、产物和时间线，展示 attempt、lease、Runner、失败阶段、取消/重试入口和 UTC 原值。
- [ ] **AF-UI-005 `P0` 实现大日志查看器。** 虚拟化或分页加载，stdout/stderr/关键字/时间筛选，安全 ANSI 渲染，明确标记截断、补传和缺口。
- [ ] **AF-UI-006 `P1` 完善 Runner 管理页面。** 展示协议/Agent/工具链版本、标签、能力、容量、负载、lease、最近任务；支持排空、禁用、凭据轮换和注销。
- [ ] **AF-UI-007 `P1` 让首页使用真实执行数据。** 替换“执行链路尚未启用”占位，显示质量、活动批次、Runner 容量、失败摘要和最近动态。
- [ ] **AF-UI-008 `P1` 完成全局搜索。** 搜索用例、任务、批次、执行和 Runner，结果按权限过滤并支持键盘导航；不再只是用例查询框。
- [ ] **AF-UI-009 `P1` 实现通知中心。** 首版使用站内持久通知，覆盖批次完成、长期阻塞、Runner 离线、同步失败和系统告警，不依赖外部邮件/SaaS。
- [ ] **AF-UI-010 `P1` 完成系统设置。** 提供只读部署模式、LDAP、成员/权限、密文、存储、保留策略、调度阈值和系统诊断，不提供误导性的 Lite/Full 在线切换。
- [ ] **AF-UI-011 `P1` 完善通用页面状态。** 所有 Bento 卡片、表格、表单和浮层覆盖 loading、empty、error、forbidden、stale/offline，并保留用户已输入内容。
- [ ] **AF-UI-012 `P1` 完成可访问性和窄屏验收。** 键盘、焦点、表单标签、状态非纯颜色、200% 缩放、减少动效和窄屏关键操作达到设计文档要求。
- [ ] **AF-UI-013 `P2` 提供深色日志模式。** 仅在 token、图表、日志安全渲染和 E2E 同时覆盖后发布，不要求全站先实现深色主题。

## 10. M7 — 分析与洞察

- [ ] **AF-ANALYTICS-001 `P1` 定义分析事实模型。** 以已确认的执行结果为来源，保存可重算的结果、耗时、失败分类和维度；重复完成不能重复计数。
- [ ] **AF-ANALYTICS-002 `P1` 实现双模式聚合任务。** Lite 通过 SQLite 持久任务增量聚合，Full 通过 outbox/JetStream 工作器聚合，均可从权威执行数据重建。
- [ ] **AF-ANALYTICS-003 `P1` 实现质量指标。** 提供成功率、失败率、跳过率、耗时分位数和时间趋势，并明确样本数、时区和计算口径。
- [ ] **AF-ANALYTICS-004 `P1` 实现多维筛选。** 支持项目、任务、用例、标签、Runner、时间范围、失败类型和环境版本，筛选状态进入 URL。
- [ ] **AF-ANALYTICS-005 `P1` 实现批次对比。** 对比用例集合、版本、环境、Runner、结果和耗时变化，避免把样本范围不同的百分比直接比较。
- [ ] **AF-ANALYTICS-006 `P1` 实现失败分类与聚类。** 先使用可解释的错误码/归一化签名分组，保留原始证据与手动修正；不依赖在线 AI 服务。
- [ ] **AF-ANALYTICS-007 `P1` 识别不稳定用例。** 定义最小样本、时间窗和 flaky 判定口径，展示置信信息并允许排除基础设施失败。
- [ ] **AF-ANALYTICS-008 `P1` 实现洞察页面。** 图表同时提供文字摘要和数据表替代，颜色语义固定，空样本和数据延迟有明确提示。
- [ ] **AF-ANALYTICS-009 `P1` 定义数据保留和重算。** 原始执行、日志、产物、聚合和审计使用分别配置的保留期，清理幂等且不破坏仍需展示的引用。
- [ ] **AF-ANALYTICS-010 `P2` 实现受控导出。** 异步生成有权限过滤、行数/大小限制和审计记录的 CSV/JSON 报表，离线环境可下载。

## 11. M8 — 可观测性、安全加固与运维

### 11.1 可观测性

- [ ] **AF-OBS-001 `P0` 统一结构化日志。** 生产日志至少包含 timestamp、level、message、requestId；执行路径补充 batch/run/attempt/runner，且控制标签基数和秘密脱敏。
- [ ] **AF-OBS-002 `P0` 贯通请求与执行关联。** API、队列、worker、Runner Protocol、Agent 诊断和审计可使用稳定 ID 追踪同一执行，但不把高基数 ID 用作指标标签。
- [ ] **AF-OBS-003 `P1` 增加运行指标。** 覆盖请求、队列深度、调度延迟、活跃 lease、Runner 容量、日志/产物上传、失败和清理任务；外部导出默认关闭。
- [ ] **AF-OBS-004 `P1` 完善 liveness/readiness。** Web、worker 和 Agent 区分存活/就绪，检查有界超时；Lite 检查数据目录/SQLite，Full 检查所需服务和迁移状态。
- [ ] **AF-OBS-005 `P1` 提供系统诊断包。** 管理员可生成有界、脱敏的版本、配置摘要、依赖健康和近期错误信息，不包含数据库口令、令牌、密文值或完整用户日志。

### 11.2 安全与数据治理

- [ ] **AF-SEC-001 `P0` 为所有 Runner API 加固边界。** 认证、协议版本、请求体上限、速率限制、重放防护、幂等键和稳定错误响应必须一致实现。
- [ ] **AF-SEC-002 `P0` 加固文件与对象下载。** 校验对象归属、权限、大小、媒体类型、摘要和内容处置；JAR、报告和日志不得被浏览器当作可执行页面内联运行。
- [ ] **AF-SEC-003 `P0` 增加 Web 安全策略。** 配置 CSP、frame、MIME、referrer、Cookie 和 TLS 代理边界，确保 xterm/WebSocket 所需策略最小化而非放宽全站。
- [ ] **AF-SEC-004 `P1` 实现数据保留与清理工作器。** 对对象、日志、产物、会话、队列死信、Agent spool 和审计分别执行可重试、有审计的策略。
- [ ] **AF-SEC-005 `P1` 建立依赖安全流程。** CI 生成并归档漏洞/许可证结果，固定依赖和镜像摘要，定义离线环境的安全更新、例外审批和升级节奏。
- [ ] **AF-SEC-006 `P1` 完成备份数据保护。** 备份中的用户凭据、LDAP bind 密文、主密钥引用、Runner 身份和业务密文有清晰的加密、访问和恢复后轮换要求。

### 11.3 运维能力

- [ ] **AF-OPS-001 `P1` 实现可配置保留策略。** 日志、产物、来源 JAR、执行数据、分析和审计分别配置，提供影响预览、最小/最大值和磁盘容量告警。
- [ ] **AF-OPS-002 `P1` 实现备份与恢复工具。** Lite 将 SQLite 与对象目录作为一致集合；Full 覆盖 PostgreSQL 与对象元数据/MinIO，记录 NATS/Redis 可重建边界并进行恢复演练。
- [ ] **AF-OPS-003 `P1` 实现迁移与升级工具。** 升级前检查、备份、迁移、失败恢复和版本兼容诊断均可离线运行，禁止生产启动时使用 schema push。
- [ ] **AF-OPS-004 `P1` 提供容量与故障运行手册。** 覆盖磁盘满、数据库锁/连接耗尽、NATS/Redis/MinIO 故障、Runner 大面积离线、积压、密钥轮换和回滚。
- [ ] **AF-OPS-005 `P2` 完成 Full 水平扩展验证。** 验证多 Web/worker、副本滚动升级、并发调度、防超卖和终端亲和/中继方案，给出支持规模与压测证据。

## 12. M9 — 离线部署与发布完整性

- [ ] **AF-OFFLINE-001 `P0` 产出 Lite 离线部署包。** 包含固定摘要的 AutoForge 镜像、Runner 二进制、Compose/服务模板、环境模板、迁移工具、校验和、SBOM、许可证和安装说明。
- [ ] **AF-OFFLINE-002 `P1` 产出 Full 离线部署清单。** 固定 PostgreSQL、NATS、S3 兼容对象存储和 Redis 的已验证版本/摘要、配置与许可证获取方式；解决 MinIO 上游归档后的维护和替代发行版策略。
- [x] **AF-OFFLINE-003 `P1` 提供 Full Compose 离线模板。** 支持从本地镜像仓库或归档导入启动，不在安装时拉取镜像、脚本或包，并包含数据卷、健康检查和初始化顺序。
- [ ] **AF-OFFLINE-004 `P1` 打包 Runner 工具链。** 提供版本化 JDK/TestNG 基线和可选浏览器/驱动 Runner 镜像或预置说明，首次执行不得访问公网下载依赖。
- [ ] **AF-OFFLINE-005 `P1` 完成安装、升级和回滚手册。** 分别覆盖 Lite/Full、四种平台资产、私有 CA、Agent 服务、迁移、兼容矩阵、校验和与来源证明验证。
- [ ] **AF-OFFLINE-006 `P1` 完成断网 E2E 验收。** 在阻断所有出站网络的环境从 Release 资产部署，完成登录、LDAP 内网登录、导入 JAR、创建任务、Agent 注册、执行、日志、产物、分析、备份和恢复。
- [ ] **AF-OFFLINE-007 `P1` 验证升级与灾难恢复。** 从上一正式版本携带真实规模夹具升级，模拟迁移失败、服务中断和对象缺失，验证回滚/恢复说明可操作。
- [ ] **AF-OFFLINE-008 `P2` 增加发布资产签名策略。** 在现有摘要和来源证明基础上，为离线介质提供可离线验证的签名、公钥分发和轮换流程。

## 13. M10 — 测试、质量门禁与文档

### 13.1 自动化测试

- [x] **AF-TEST-001 `P0` 完成执行领域竞态测试。** 覆盖重复 claim/complete、租约续期/过期、取消/完成、超时/完成、迟到结果、重试上限和批次聚合。
- [x] **AF-TEST-002 `P0` 完成队列适配器契约测试。** 同一套行为分别运行 SQLite queue 与 JetStream，覆盖至少一次、幂等、死信、崩溃和排空。
- [x] **AF-TEST-003 `P0` 完成 Agent 协议兼容测试。** 相邻 Agent/Server 版本兼容，不兼容版本明确拒绝；同一 Agent 分别连接 Lite 和 Full。
- [x] **AF-TEST-004 `P0` 完成 Agent 安全测试。** 覆盖注入、越界 cwd、环境泄漏、恶意文件名/符号链接、失效凭据、资源超限和进程树清理。
- [x] **AF-TEST-005 `P0` 完成日志/spool 测试。** 覆盖中文跨块、交错流、重复、缺口、断线、重传、截断、磁盘满、重启恢复和秘密脱敏。
- [x] **AF-TEST-006 `P0` 完成产物测试。** 覆盖空匹配、必需项缺失、超限、路径穿越、重复上传、摘要不匹配和对象存储故障恢复。
- [x] **AF-TEST-007 `P0` 完成 RBAC/LDAP 集成测试。** 使用离线目录夹具覆盖角色矩阵、组映射、禁用、会话撤销、跨租户访问和目录失联。
- [ ] **AF-TEST-008 `P0` 完成 Lite 核心 E2E。** 创建用户/权限 → 导入 JAR → 创建任务 → Agent 注册 → 单个/批量执行 → 日志/产物 → 失败重试 → 分析形成闭环。
- [ ] **AF-TEST-009 `P1` 完成 Full 核心 E2E。** 使用真实 PostgreSQL、JetStream、MinIO/S3 和 Redis 验证相同闭环、重复消息和依赖短暂故障恢复。
- [ ] **AF-TEST-010 `P1` 增加性能与稳定性测试。** 覆盖大 JAR、有界高用例数、多 Runner、日志吞吐、调度公平、数据库竞争、队列积压和长时间运行，给出支持规模。
- [ ] **AF-TEST-011 `P1` 增加无公网验收门禁。** CI/发布候选环境阻断出站网络并确认运行期没有 CDN、遥测、下载或许可证请求。
- [ ] **AF-TEST-012 `P1` 增加备份恢复和迁移测试。** SQLite/PostgreSQL 分别验证全新建库、上一版本升级、失败恢复和对象一致性。

### 13.2 文档与交付标准

- [ ] **AF-DOC-001 `P1` 提供管理员手册。** 覆盖部署模式、初始管理员、LDAP、用户/角色、密文、Runner、终端、保留、备份恢复和故障排查。
- [ ] **AF-DOC-002 `P1` 提供用户手册。** 覆盖 JAR 来源、用例任务、环境、单次/批量执行、日志、产物和洞察，并解释每个执行状态。
- [ ] **AF-DOC-003 `P1` 提供 Runner 运维手册。** 覆盖离线安装、私有 CA、注册、工具链、能力、升级回滚、spool、资源限制、排空和诊断。
- [ ] **AF-DOC-004 `P1` 发布 API/Runner Protocol 文档。** 从版本化 schema 生成或校验示例，记录认证、权限、限流、幂等、分页、错误码和兼容政策。
- [ ] **AF-DOC-005 `P1` 建立变更日志和兼容矩阵。** 每个 Release 记录数据库迁移、配置变化、Agent/Server 兼容、离线资产变化和已知限制。
- [ ] **AF-DOC-006 `P1` 保持状态文档一致。** 每完成一个事项同步检查 README、AGENTS、架构设计、运维文档、`.env.example` 和页面中的“规划中”提示。

## 14. 阶段验收门

只有满足对应验收门，才可将里程碑对外标记为完成：

- [ ] **Gate A — 安全可登录。** 本地身份、LDAP、用户生命周期、RBAC、项目数据隔离和审计在 Lite/Full 均通过；所有 Web/API/WebSocket 入口默认拒绝未授权访问。
- [ ] **Gate B — Lite 执行闭环。** 无 PostgreSQL/NATS/MinIO/Redis 时，Agent 能领取、续租、执行 TestNG、上传日志/产物、重试失败并在崩溃后恢复。
- [ ] **Gate C — Full 执行闭环。** PostgreSQL、JetStream、S3/MinIO、Redis 和独立 worker 支持至少一次交付、幂等处理、水平扩展和依赖短暂故障恢复。
- [ ] **Gate D — 产品闭环。** 用例/任务/环境、单次与批量执行、Runner 管理、日志/产物、分析、审计和系统设置都有可用 UI 与权限控制。
- [ ] **Gate E — 离线生产验收。** 从不可变 Release 资产在断网环境完成安装、核心业务、升级、备份恢复和 Agent 工具链执行，全部校验和、SBOM、许可证和运维文档齐备。
