name: CI

on:
  pull_request:
  push:
    branches:
      - "**"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-quality:
    name: Formatting, lint and types
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Validate source and E2E coverage inventory
        run: |
          pnpm format:check
          pnpm lint
          pnpm typecheck
          pnpm test:e2e:matrix

  unit-integration:
    name: Unit, integration and performance
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Run automated tests
        run: |
          pnpm test
          pnpm test:integration
          pnpm test:performance
          mvn --batch-mode --file adapters/cotest-testng/pom.xml test

  lite-browser:
    name: Lite browser · ${{ matrix.scenario }}
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        include:
          - scenario: assets
            specs: tests/e2e/case-suite-lifecycle.spec.ts tests/e2e/jar-import.spec.ts tests/e2e/project-isolation.spec.ts
            project_maximum_concurrency: ""
            screenshots: false
          - scenario: execution
            specs: tests/e2e/execution-recovery.spec.ts tests/e2e/single-case-run.spec.ts
            project_maximum_concurrency: "1"
            screenshots: false
          - scenario: identity
            specs: tests/e2e/identity-rbac.spec.ts tests/e2e/all-rounds.spec.ts
            project_maximum_concurrency: ""
            screenshots: false
          - scenario: operations
            specs: tests/e2e/management-operations.spec.ts tests/e2e/platform-operations.spec.ts tests/e2e/scheduling-refill.spec.ts tests/e2e/ui-layout.spec.ts
            project_maximum_concurrency: ""
            screenshots: true
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run isolated Lite browser scenario
        env:
          AUTOFORGE_E2E_DATA_DIR: ${{ runner.temp }}/autoforge-${{ matrix.scenario }}
          AUTOFORGE_UI_SCREENSHOT_DIR: ${{ matrix.screenshots && format('{0}/autoforge-ui-screenshots', runner.temp) || '' }}
          E2E_PROJECT_MAXIMUM_CONCURRENCY: ${{ matrix.project_maximum_concurrency }}
          E2E_SCENARIO_SPECS: ${{ matrix.specs }}
        run: |
          read -r -a scenario_specs <<< "${E2E_SCENARIO_SPECS}"
          pnpm exec playwright test "${scenario_specs[@]}"
      - name: Upload serial UI screenshots
        if: always() && matrix.screenshots
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ui-layout-screenshots-${{ github.sha }}
          path: ${{ runner.temp }}/autoforge-ui-screenshots
          if-no-files-found: warn
          retention-days: 7
      - name: Upload browser trace on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: browser-trace-${{ matrix.scenario }}
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  build:
    name: Production platform and Agent build
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Build immutable production outputs
        run: pnpm build

  verification-image:
    name: Current SHA amd64 verification image
    if: >-
      github.event_name == 'push' &&
      contains(github.event.head_commit.message, '[verification-image]')
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      AUTOFORGE_BUILDX_CACHE_FROM: type=gha,scope=ci-verification-backend-amd64,timeout=2m
      AUTOFORGE_BUILDX_CACHE_TO: type=gha,mode=max,scope=ci-verification-backend-amd64,timeout=2m,ignore-error=true
      AUTOFORGE_KEEP_DOCKER_TAR: "1"
      AUTOFORGE_RELEASE_REVISION: ${{ github.sha }}
      BACKEND_IMAGE: autoforge/backend:0.0.0-ci.${{ github.run_id }}-amd64
      BACKEND_PLATFORM: linux/amd64
      VERIFICATION_VERSION: 0.0.0-ci.${{ github.run_id }}
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go for embedded Agent resources
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Record reproducible build time
        run: echo "SOURCE_DATE_EPOCH=$(git show -s --format=%ct HEAD)" >> "${GITHUB_ENV}"
      - name: Build compressed verification image
        run: >-
          bash scripts/release/build-backend-image.sh
          "${VERIFICATION_VERSION}" amd64 dist/verification
      - name: Load and verify image
        run: |
          docker load --input "dist/verification/autoforge-backend-${VERIFICATION_VERSION}-amd64.docker.tar"
          bash scripts/release/verify-backend-image.sh "${BACKEND_IMAGE}" "${BACKEND_PLATFORM}"
          rm -- "dist/verification/autoforge-backend-${VERIFICATION_VERSION}-amd64.docker.tar"
      - name: Upload compressed verification image
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: verification-backend-amd64-${{ github.sha }}
          path: dist/verification/*.docker.tar.zst
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  real-agent-acceptance:
    name: Network-blocked real Agent acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run real Agent acceptance without outbound network
        run: pnpm test:real-agent
      - name: Upload browser and Agent diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: real-agent-acceptance-diagnostics
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  batch-input-sharing-acceptance:
    name: Batch-shared Runner inputs
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Verify shared downloads, extraction and cleanup
        run: pnpm test:batch-input-sharing
      - name: Upload browser and Agent diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: batch-input-sharing-diagnostics
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  container-executor-acceptance:
    name: Immutable container executor acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run immutable container executor acceptance
        run: pnpm test:container-executor
      - name: Upload browser and Agent diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: container-executor-acceptance-diagnostics
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  ldap-acceptance:
    name: Network-blocked real LDAP acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run real LDAP acceptance without service egress
        run: pnpm test:ldap-e2e
      - name: Upload browser trace on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ldap-acceptance-diagnostics
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  runner-install-acceptance:
    name: Offline SSH and systemd Runner installation
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run SSH installation acceptance without service egress
        run: pnpm test:runner-install-e2e
      - name: Upload browser trace on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: runner-install-acceptance-diagnostics
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  system-acceptance:
    name: ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Full · runtime Agent
            command: bash scripts/quality/test-full-business-recovery.sh runtime-agent
          - name: Full · runtime recovery
            command: bash scripts/quality/test-full-business-recovery.sh runtime-recovery
          - name: Full · asset lifecycle
            command: bash scripts/quality/test-full-business-recovery.sh browser-assets
          - name: Full · governance
            command: bash scripts/quality/test-full-business-recovery.sh browser-governance
          - name: Network-blocked Lite · assets
            command: bash scripts/quality/test-offline.sh assets
          - name: Network-blocked Lite · governance
            command: bash scripts/quality/test-offline.sh governance
          - name: Network-blocked Lite · operations and deployment
            command: bash scripts/quality/test-offline.sh operations && pnpm test:deployment
          - name: Network-blocked Lite · recovery
            command: bash scripts/quality/test-offline.sh recovery
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.16.0
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
          cache-dependency-path: apps/runner-agent/go.mod
      - name: Install locked dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Locate preinstalled Chromium
        run: |
          browser_path="$(command -v google-chrome || command -v chromium || true)"
          if [[ -z "${browser_path}" ]]; then
            echo "The hosted runner does not provide a preinstalled Chromium browser." >&2
            exit 1
          fi
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=${browser_path}" >> "${GITHUB_ENV}"
      - name: Run system acceptance
        run: ${{ matrix.command }}
      - name: Upload browser trace on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: system-acceptance-diagnostics-${{ matrix.name }}
          path: test-results
          if-no-files-found: ignore
          retention-days: 7

  ci-gate:
    name: CI gate
    if: always()
    needs:
      [
        static-quality,
        unit-integration,
        lite-browser,
        build,
        real-agent-acceptance,
        batch-input-sharing-acceptance,
        container-executor-acceptance,
        ldap-acceptance,
        runner-install-acceptance,
        system-acceptance,
      ]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Require every parallel gate
        run: |
          test "${{ needs.static-quality.result }}" = success
          test "${{ needs.unit-integration.result }}" = success
          test "${{ needs.lite-browser.result }}" = success
          test "${{ needs.build.result }}" = success
          test "${{ needs.real-agent-acceptance.result }}" = success
          test "${{ needs.batch-input-sharing-acceptance.result }}" = success
          test "${{ needs.container-executor-acceptance.result }}" = success
          test "${{ needs.ldap-acceptance.result }}" = success
          test "${{ needs.runner-install-acceptance.result }}" = success
          test "${{ needs.system-acceptance.result }}" = success
