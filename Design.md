# AutoForge 产品界面设计说明

本文是当前产品界面的实现约束与后续审视清单。视觉基线仍以
[`docs/design/autoforge-apple-like-e-dashboard.png`](./docs/design/autoforge-apple-like-e-dashboard.png)
和 [`docs/design/frontend-design.md`](./docs/design/frontend-design.md) 为准；设计图负责表达信息层级，真实页面必须使用权威业务数据与可操作组件，不能把设计图当作背景图片。

## 1. 本轮审视结论

此前首页的问题不是单纯的颜色或间距偏差，而是信息架构偏离：页面把多个低频平台计数放在首屏，却没有直接回答用户最常见的六个问题——本周质量如何、是否有执行在进行、用例资产规模如何、执行资源是否充足、失败集中在哪里、最近发生了什么。执行入口又被放在独立导航页，使用户在任何工作上下文中都要先离开当前页面。

本轮实现将首页恢复为设计稿中的六块工作台，并将数据来源明确为：

| 区域     | 用户问题                 | 权威数据                         |
| -------- | ------------------------ | -------------------------------- |
| 本周质量 | 最近七天质量是否改善     | 已确认 TestNG 方法结果与趋势聚合 |
| 活动执行 | 当前批次进行到哪里       | `RunBatch` 实时聚合              |
| 用例库   | 资产规模和可执行方法数量 | 用例目录与来源统计               |
| 执行机组 | 资源池和剩余槽位是否充足 | Runner、Runner Group 与心跳容量  |
| 失败洞察 | 主要失败集中在哪些原因   | 权威错误描述聚类                 |
| 最近动态 | 最近有哪些执行或导入事件 | 批次与来源的更新时间线           |

空数据不再使用伪造数字。每块卡片展示明确的空状态及下一步入口。

## 2. 全局信息架构

### 一级导航

一级导航包含高频工作区：`工作概览`、`用例管理`、`用例任务`、`执行记录`、`执行节点`、`质量洞察`。执行动作不是一个信息页面，因此“用例批跑”不再占用一级导航。管理入口数量已经收敛，不再用只有一两个子项的折叠组增加操作层级；权限允许的管理目的地全部以一级入口直接展示。

侧栏的全部一级入口都使用四个汉字，形成稳定的扫描节奏；“首页”“用例库”等短标签不得在视觉改版时改回，也不得重新引入折叠式二级管理导航。该规则由 `apps/web/src/components/ui-usage.test.ts` 的静态结构测试覆盖，并随根级 `pnpm test` 在 CI 执行。新增或重命名侧栏入口时，必须先选择含义准确的四字名称，不能通过关闭或绕过测试解决命名冲突。

### 全局动作

顶栏的“开始执行”是全局主按钮，在所有有 `run.create` 权限的业务页面可见。它打开统一弹窗，支持：

1. 任务模式只选择一个已配置任务，确认后直接执行；Runner/Runner Group、项目版本、
   Adapter 环境地址、重跑与恢复时限均来自任务的最新不可变快照；
2. 单用例模式允许临时指定一台或多台执行机，或选择一个执行机组；
3. 单用例默认启用 CoTest Adapter，可配置失败重跑方式、Suite、Test 和执行环境 IP/地址；
4. 两种模式都复用服务端预检与权威调度状态机。

项目、项目版本和测试阶段共同组成顶栏全局状态，不再由每个列表、导入页或管理页分别维护。服务端以
当前登录会话的三个 HttpOnly Cookie 保存层级 ID，并在每次切换时校验项目权限、版本状态以及阶段与
版本的从属关系；页面再按自身权限做第二次作用域收窄。切换任一层级会清除已失效的分页和旧页面层级
参数，同时保留与项目无关的筛选条件。用例目录、质量统计、单用例选择和 TestNG JAR 导入必须消费同一
层级；导入页只显示只读目标摘要，不能再提供局部版本或阶段覆盖。

用例管理的列表导入支持 `.xlsx`、`.csv`、`.tsv` 和 `.txt`，只用于按路径批量勾选当前层级已有
用例。XLSX 读取首个工作表第一列，并在用户选择文件后才加载离线打包的解析器；文本列表依次识别
UTF-8、带 BOM 的 UTF-16 和 GB18030，不能再把二进制工作簿或中文 Windows CSV 当作 UTF-8
文本显示。输入限制为 32 MiB，首个工作表最多 100,001 行（含可选表头），保持 10 万级用例选择
能力并对空文件、旧版 `.xls`、加密或损坏工作簿给出可操作错误。

用例目录中的勾选同时服务于加入任务和批量删除，但权限分别按 `case_suite.manage` 与 `case.manage`
计算，不能因为有任务编辑权限就获得用例删除权限。低频删除操作放在选择工具条和右侧详情的“管理用例”
折叠区，执行前明确提示影响范围；删除后目录即时收敛，不保留空白占位。单个和批量删除都会清理当前
用例版本、方法与任务成员关系，既有执行和质量分析记录继续作为历史快照保留。

`/run-batches` 只兼容重定向到执行记录，不再承载规划表单；批次详情继续使用
`/run-batches/{batchId}` 并归属“执行记录”。任务不再配置重复的“执行超时”，用例进程时限只读取
平台配置中的“用例执行超时”；任务仍可配置排队、领取和上传恢复时限，这三者分别约束调度等待、
assignment 领取和产物收尾，不会与用例进程时限冲突。

### 管理导航

管理目的地以`项目管理`、`访问管理`、`运维计划`、`执行机组`、`安全审计`、`平台设置`、`文件来源`
七个一级入口直接展示。访问管理和平台设置继续使用页内四字标签合并关联配置，避免侧栏为同一路由
重复占位；侧栏不再保留“项目协作 / 身份权限 / 执行配置 / 平台运维”折叠层。

入口仍按权限逐项过滤；没有权限的目的地不渲染。
产品级“执行环境”和“执行密文”已退役，导航、页面、API 和任务编辑表单都不再暴露这两类配置；
LDAP、Runner 凭据、API Key 等基础设施身份秘密仍由各自的安全边界管理，不能与已退役功能混为一谈。

## 3. 首页布局

`>= 1440px` 使用四列 Bento：本周质量和执行机组各跨两列，其余四块各占一列。中等桌面降为三列，1024–1180px 降为两列。工作台宽度随可用主内容区增长，在 4K 视口仍至少使用主内容区的 85%，不会退化成居中的窄小卡片岛。

当前产品边界是宽度不小于 1024px 的桌面浏览器。响应式设计覆盖 1024px、常规桌面、2K、4K 和超宽屏，不实现也不验收手机端布局；后续编码代理不得把手机兼容作为默认完成条件扩张范围。

卡片采用相同的标题、边界、圆角和阴影语义，但内部密度按任务调整：趋势卡允许更多纵向空间，状态列表和最近动态保持紧凑。颜色只表达状态：蓝色代表信息/运行，绿色代表成功/在线，橙红代表失败或风险，紫色代表等待或补充维度。

设计稿中的图表均由真实数据生成，并提供文本标签、数值和空状态；图形不能成为理解数据的唯一途径。

## 4. 质量统计与失败洞察

通过率、失败率和每日趋势统一按 TestNG 方法结果计算：分母为 `passed + failed + skipped`，每日总数也必须等于当日三类方法数之和，不能混用执行 attempt 数。当前 Runner Protocol 的 TestNG 结果把计数放在顶层；聚合器同时兼容历史 `summary` 包裹格式，旧的错误分析事实会按 schema 版本自动重建。

`resultCode` 是调度与诊断使用的机器分类码，不是面向用户的失败原因。失败洞察展示 `RunAttempt.resultSummary` 中由 Adapter 失败堆栈提取的错误描述；生成内部聚类签名时只对该描述做 ID、数字和空白归一化。成功结果（包括 `TESTNG_SUCCEEDED` 及其兼容码）绝不能生成失败签名或进入失败统计。缺少描述的异常终止使用明确的兜底说明，而不是把结果码冒充错误描述。

洞察页打开后先显示执行样本、方法通过率、方法失败率、P95 耗时、每日趋势、失败原因和不稳定用例；最多 500 行的当前层级用例明细与批次对比放在统计之后，不能再把统计结果挤出首屏。趋势图按实际天数收缩，不用少量柱子撑出大块空白；失败原因列表限制高度和长文本行数，完整内容保留在 `title`；不稳定用例显示业务名称并链接详情，UUID 仅作为路由标识。趋势图下方保留精确数字表格，图形不是唯一信息载体。统计查询和导出同时受顶栏项目、版本与测试阶段约束。

## 5. 执行机组

Runner Group 是可复用资源池，而不是动态查询或标签别名。组保存名称、说明、成员 Runner ID、修订号和时间；Lite 使用 SQLite，Full 使用 PostgreSQL，两种适配器共享同一仓储契约。

执行创建时，服务端读取组成员并将排序后的 Runner ID 固化为批次快照。后续修改或删除组不会改变历史批次，也不会在任务运行中悄悄扩缩资源范围。空组允许保存以便提前配置，但不能发起执行；创建或更新组时拒绝不存在和已清除的成员，发起执行时再拒绝已禁用或能力不兼容的成员。更新使用修订号防止覆盖其他管理员的修改。

## 6. 执行结果与可靠性

批次详情在真实轮次前提供“总结”虚拟轮次。总用例数始终等于初始轮次的用例数；任一轮曾通过的用例只计一次通过；从未通过的用例使用它的最后一次尝试作为最终失败、超时或取消结果。总结列表因此严格保留每个初始用例一行，不会把同一用例的多次 attempt 重复计入分母。

Runner 或传输基础设施异常（例如 `PROCESS_START_FAILED`、`LOG_UPLOAD_FAILED`、lease 失效）与用例断言失败使用独立预算。每个用例最多自动重调度两次，不消耗用户设置的用例失败重跑次数；即使选择整轮重跑，基础设施异常也立即重调度。调度器优先避开已对该用例产生过基础设施异常的 Runner；仅有一台合格 Runner 时允许有界回退，避免小型部署永久卡住。断言、用例配置、用户取消和用例资源上限不属于 Runner 异常，不可盲目换机重试。

每次自动重调度都写入持久调度事件。真实轮次的“执行机”页签提供“执行机异常”弹窗，按执行机、结果码和压缩后的错误描述聚合展示出现次数、受影响用例和最近时间，使管理员可在重调度保证进度的同时发现故障节点。这些可恢复异常不进入 TestNG 通过率、失败洞察或不稳定用例统计。

容量计算覆盖 `queued`、`dispatching`、`scheduled` 和 `running` 全部非终态批次；首次 claim 前已经预留的槽位同样占用容量，领取触发的补调度不能重复分配这些槽位。终态批次的历史异常 attempt 不再占用实时容量。

## 7. 执行机安装与更新

首次 SSH 安装或手动更新成功后，控制面使用平台主密钥的 AES-256-GCM 将主机地址、端口、用户名、密码和可选私有 CA 作为一个带 AAD 的版本化密文保存。列表 API 只解密并返回安全摘要（主机、端口、用户名、已保存密码标记），密码和 CA 永不返回浏览器。主机指纹仍是必需校验边界，使用已保存连接不会降级 SSH 主机身份确认。

保存的连接档案可用于不再输入密码的再安装和单机更新。批量更新一次最多选择 50 台、并发度为 4，每台独立返回更新成功、缺少连接档案或失败，单台断线不会中止其他节点。两种数据库都保存同一语义的连接档案；Runner 注销后档案仍保留并可用于再安装，记录被彻底清理后档案才解除绑定，后续同名注册会重新建立关联。

## 8. 组件和视觉约束

- 使用现有语义 token，禁止在业务组件中复制一套颜色、阴影或圆角体系。
- 表单控件默认 40px 高；数据表保持更紧凑的 32–36px 行高。
- 主按钮每个视区原则上只有一个；破坏性操作使用危险色且保留确认。
- 项目、版本和测试阶段切换只出现在顶栏；页面中的同名字段只允许表达权限分配或持久业务配置等输入，不得重新承担页面上下文切换。
- 创建项目、用户、角色、任务、执行机组、服务账号、项目版本和测试阶段，以及密码重置、角色分配、任务复制等低频动作使用弹窗；搜索、筛选、频繁编辑和需要持续对照列表的配置保留在页面中。
- 用例目录和任务成员树的文件夹节点支持递归整选、取消与半选状态；一次选择作用于完整后代，不以当前已渲染的 250 行为边界。大目录、包和用例按 250 行渐进渲染，避免 10 万级资产一次生成 10 万个 DOM 节点。
- 任务不设置低于 10 万用例的产品容量上限；HTTP 请求仍有 16 MiB/10 万 ID 的单次安全边界，客户端和仓储用分批传输/SQL 完成完整操作。批量移除必须在一个仓储事务中只生成一个任务版本。
- 全局执行弹窗通过 Portal 挂载到 `body`，遮罩覆盖完整视口且弹窗以视口几何中心定位，不能受顶栏的 `backdrop-filter` 或其他包含块限制。
- 弹窗最大高度受视口约束，主体滚动，操作栏保持可见；`Escape`、遮罩和关闭按钮都可退出，提交期间避免误关。
- 长名称使用省略号并保留完整 `title` 或详情入口，不让单个极端值决定整列宽度。
- 执行记录表按每列内容长度的 70 分位计算初始列宽，至少完整容纳 70% 的常见行；
  `<table>` 的明确像素宽度必须等于当前 `<colgroup>` 列宽之和并使用 `table-layout: fixed`，不能使用 `max-content` 或内容自动布局；否则真实浏览器仍会让一个极端长单元格重算整列。极端长值在有上限的列内省略，用户手动拖拽后的宽度继续按浏览器持久化。
- 执行详情的状态排序先按状态类别，再以失败错误描述（失败堆栈摘要）排列失败行；
  错误码只在没有描述时兜底，不能让同一机器码下的所有失败退化为按用例名排序。
- 筛选、分页、详情选择等可恢复状态优先进入 URL；短暂的弹窗表单草稿可留在本地组件状态。
- 所有图标来自锁定版本的离线包；图标不能替代可访问名称。

## 9. 数据表与详情页后续优化顺序

本轮优先解决首页、全局执行和执行机组。其余页面继续按以下顺序审视，避免无边界重写：

1. 统一列表页标题区、筛选栏和计数位置；
2. 将用例库、Runner 列表的常规行高收敛到专业密度；
3. 将执行记录已采用的 70 分位列宽、手动调整和长内容详情入口推广到其余高频表格；
4. 清理仍使用旧式大留白或重复卡片标题的设置页；
5. 补齐 1024px 下的侧栏收起与 Inspector 抽屉，不把横向裁切当作桌面响应式；
6. 在 CI 生成固定视口截图并做人工审阅，视觉变化不依赖本机高资源构建。

## 10. 可访问性与验证

键盘焦点必须可见，原生表单语义不能被自绘组件移除；弹窗使用 `role="dialog"`、`aria-modal` 和可访问名称。状态同时使用文字、图标和颜色。时间显示采用用户时区，权威数据继续保存 UTC。

自动化还必须覆盖顶栏项目/版本/阶段跨页持久化、禁止页面级层级切换和手工执行参数回归、统计先于长明细展示、任务成员树批量移除，以及低频管理动作弹窗。

当前自动化证据包括：四字侧栏命名的 CI 静态约束、Runner Group 应用服务单元测试、SQLite/PostgreSQL 分析事实重建、执行记录 70 分位列宽与失败描述排序测试、调度与 Adapter 地址固化测试、10 万用例目录整选、Lite/Full 10 万任务成员与执行批次持久化、1024px 到 4K 的首页宽度断言，以及全局弹窗完整遮罩和几何居中的 Playwright 断言。浏览器资产场景会截取用例文件夹整选、任务包整选、不含执行参数输入的任务策略区和任务复制弹窗；固定视口场景还会截取顶栏全局项目上下文、默认启用 CoTest Adapter 且不含参数输入的单用例弹窗，以及创建任务/执行机组/项目/用户/角色/服务账号、成员与角色分配、负责人转移、密码重置、项目版本和测试阶段等全部低频管理弹窗。CI 截图环境显式安装 CJK 字体，避免中文方框使人工审图失效。截图产物按场景上传供人工审阅，但不是像素差异基线，不能表述为所有交互状态均已自动视觉回归。浏览器轮次场景额外向真实 DOM 注入单行极端长值，确认表格和任务列的几何宽度不变；同一场景用用户重跑上限为 0 的 `PROCESS_START_FAILED` 验证独立重调度、总结轮次和执行机异常弹窗。真实协议 E2E 会写入同一用例的一次 TestNG 失败和一次成功，验证首页与洞察页均显示 50% 方法通过率、错误描述来自失败堆栈、成功码不进入失败原因；独立真实 Agent E2E 使用中英混合的 `Assert.assertTrue` 消息验证 Adapter、日志上传和控制面摘要全链路保持 UTF-8；SSH/systemd E2E 确认连接密文不包含原密码、安全摘要不回传密码并能用保存档案批量更新。上述常规浏览器场景已进入根级 `pnpm test:e2e`；真实 Agent、Full 基础设施和离线发布验收继续使用各自独立的高资源质量门禁。

同一浏览器轮次场景还签发最小权限 API Key，分别调用 Jenkins 依赖替换与任务执行接口，验证
30 秒轮询契约、任务完整生命周期，以及带签名参数的免登录进展页不渲染顶栏和侧栏。两个 Jenkins
HPI 在独立 CI Job 构建，Release 将插件、各自 SBOM、校验和与清单一起发布。
