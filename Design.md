# AutoForge 产品界面设计说明

本文是当前产品界面的实现约束与后续审视清单。视觉基线仍以
[`docs/design/autoforge-apple-like-e-dashboard.png`](./docs/design/autoforge-apple-like-e-dashboard.png)
和 [`docs/design/frontend-design.md`](./docs/design/frontend-design.md) 为准；设计图负责表达信息层级，真实页面必须使用权威业务数据与可操作组件，不能把设计图当作背景图片。

## 1. 本轮审视结论

此前首页的问题不是单纯的颜色或间距偏差，而是信息架构偏离：页面把多个低频平台计数放在首屏，却没有直接回答用户最常见的六个问题——本周质量如何、是否有执行在进行、用例资产规模如何、执行资源是否充足、失败集中在哪里、最近发生了什么。执行入口又被放在独立导航页，使用户在任何工作上下文中都要先离开当前页面。

本轮实现将首页恢复为设计稿中的六块工作台，并将数据来源明确为：

| 区域     | 用户问题                 | 权威数据                         |
| -------- | ------------------------ | -------------------------------- |
| 本周质量 | 最近七天质量是否改善     | 已确认 TestNG 方法结果与趋势聚合 |
| 活动执行 | 当前批次进行到哪里       | `RunBatch` 实时聚合              |
| 用例库   | 资产规模和可执行方法数量 | 用例目录与来源统计               |
| 执行机组 | 资源池和剩余槽位是否充足 | Runner、Runner Group 与心跳容量  |
| 失败洞察 | 主要失败集中在哪些签名   | 失败结果聚类                     |
| 最近动态 | 最近有哪些执行或导入事件 | 批次与来源的更新时间线           |

空数据不再使用伪造数字。每块卡片展示明确的空状态及下一步入口。

## 2. 全局信息架构

### 一级导航

一级导航只保留高频工作区：`首页`、`用例库`、`用例任务`、`执行记录`、`执行机`、`洞察`。执行动作不是一个信息页面，因此“用例批跑”不再占用一级导航。

### 全局动作

顶栏的“开始执行”是全局主按钮，在所有有 `run.create` 权限的业务页面可见。它打开统一弹窗，支持：

1. 选择用例任务或单个用例；
2. 指定一台或多台执行机，或选择一个执行机组；
3. 设置重跑方式、超时、受管环境或内联变量；
4. 单用例参数覆盖，以及 CoTest Adapter 的 Suite、Test 和执行环境 IP/地址；
5. 复用服务端预检与权威调度状态机。

历史 `/run-batches` 规划页暂时保留为兼容路由，不再作为产品导航入口。批次详情继续归属“执行记录”。

### 管理导航

管理入口按任务上下文拆成四组，避免把十余个页面塞进两个含义模糊的分类：

- 项目协作：项目管理、项目角色；
- 身份与访问：用户管理、角色权限、目录配置、登录会话；
- 执行配置：自动化任务、执行机组、执行环境、密文管理；
- 平台运维：安全审计、平台配置、服务账号、数据保留、系统诊断、文件来源。

分组默认折叠，当前路由所属分组自动展开；权限过滤后为空的分组不渲染。

## 3. 首页布局

`>= 1440px` 使用四列 Bento：本周质量和执行机组各跨两列，其余四块各占一列。中等桌面降为三列，1024–1180px 降为两列。页面仍遵守桌面产品的 1024px 最小宽度，不在本轮宣称移动端已经完成。

卡片采用相同的标题、边界、圆角和阴影语义，但内部密度按任务调整：趋势卡允许更多纵向空间，状态列表和最近动态保持紧凑。颜色只表达状态：蓝色代表信息/运行，绿色代表成功/在线，橙红代表失败或风险，紫色代表等待或补充维度。

设计稿中的图表均由真实数据生成，并提供文本标签、数值和空状态；图形不能成为理解数据的唯一途径。

## 4. 执行机组

Runner Group 是可复用资源池，而不是动态查询或标签别名。组保存名称、说明、成员 Runner ID、修订号和时间；Lite 使用 SQLite，Full 使用 PostgreSQL，两种适配器共享同一仓储契约。

执行创建时，服务端读取组成员并将排序后的 Runner ID 固化为批次快照。后续修改或删除组不会改变历史批次，也不会在任务运行中悄悄扩缩资源范围。空组允许保存以便提前配置，但不能发起执行；创建或更新组时拒绝不存在和已清除的成员，发起执行时再拒绝已禁用或能力不兼容的成员。更新使用修订号防止覆盖其他管理员的修改。

## 5. 组件和视觉约束

- 使用现有语义 token，禁止在业务组件中复制一套颜色、阴影或圆角体系。
- 表单控件默认 40px 高；数据表保持更紧凑的 32–36px 行高。
- 主按钮每个视区原则上只有一个；破坏性操作使用危险色且保留确认。
- 弹窗最大高度受视口约束，主体滚动，操作栏保持可见；`Escape`、遮罩和关闭按钮都可退出，提交期间避免误关。
- 长名称使用省略号并保留完整 `title` 或详情入口，不让单个极端值决定整列宽度。
- 筛选、分页、详情选择等可恢复状态优先进入 URL；短暂的弹窗表单草稿可留在本地组件状态。
- 所有图标来自锁定版本的离线包；图标不能替代可访问名称。

## 6. 数据表与详情页后续优化顺序

本轮优先解决首页、全局执行和执行机组。其余页面继续按以下顺序审视，避免无边界重写：

1. 统一列表页标题区、筛选栏和计数位置；
2. 将执行记录、用例库、Runner 列表的常规行高收敛到专业密度；
3. 为高频表格统一 70 分位列宽、手动调整和长内容详情入口；
4. 清理仍使用旧式大留白或重复卡片标题的设置页；
5. 补齐 1024px 下的侧栏收起与 Inspector 抽屉，不把横向裁切当作响应式；
6. 在 CI 生成固定视口截图并做人工审阅，视觉变化不依赖本机高资源构建。

## 7. 可访问性与验证

键盘焦点必须可见，原生表单语义不能被自绘组件移除；弹窗使用 `role="dialog"`、`aria-modal` 和可访问名称。状态同时使用文字、图标和颜色。时间显示采用用户时区，权威数据继续保存 UTC。

当前自动化证据包括：Runner Group 应用服务单元测试、SQLite/PostgreSQL 共用仓储契约、调度与 Adapter 地址固化测试、首页/导航布局断言，以及通过全局弹窗选择执行机组并领取单用例 assignment 的 Playwright 场景。上述浏览器场景已进入根级 `pnpm test:e2e`，在具备锁定版本 Chromium 的本机与 GitHub Actions 均可运行；真实 Agent、Full 基础设施和离线发布验收继续使用各自独立的高资源质量门禁。
