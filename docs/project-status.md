# AutoForge 实现进展

> 审计日期：2026-08-13。任务定义和勾选状态以 [`Todo.md`](../Todo.md) 为权威来源；E2E 完整性以 [`coverage-matrix.json`](../tests/e2e/coverage-matrix.json) 为权威来源。

## 总体进度

路线图共 207 项，已完成 188 项：M0–M10 的 175 个编号事项、M11 的 9 个页面可见性事项及 Gate A–D 已完成。18 个 M11 E2E 事项和 Gate E 保持未完成；覆盖矩阵目前将 18 项均标记为 `partial`，不会把页面访问、直接协议模拟或 readiness 检查计作完整生产验收。

| 范围 | 状态 | 主要证据 |
| --- | --- | --- |
| M0–M5 核心平台 | 完成 | 双数据库、双对象存储、双队列、Runner Protocol 与 Go Agent 测试 |
| M6–M10 产品与交付实现 | 完成 | 方案 E UI、分析、运维、离线交付候选与文档 |
| M11 页面可见性 | 完成 | 24 个页面、权限驱动导航、项目作用域、账号/成员/审计/运维/版本入口 |
| M11 完整 E2E | 部分覆盖 | 13 个分域 Playwright 场景及真实 Agent、LDAP、SSH、Container 专项；缺口见覆盖矩阵 |
| Gate E 正式 Release 验收 | 待版本标签 | 需不可变 Release 资产、升级/回滚和断网实机证据 |

## 页面与权限审计

- 所有登录用户可从顶栏进入账号安全页；强制改密账号只保留该闭环，LDAP 账号显示只读密码说明。
- 主导航和设置页标签按实际权限生成；项目、环境或计划账号不会再看到指向无权限页面的入口，`secret.manage` 可独立进入环境与密文页。
- 用例、JAR 导入、来源和用例任务均提供项目选择并把项目作用域传到列表、创建、详情和批量关联操作。
- 用户、系统/项目角色、项目成员、负责人转移、服务账号生命周期、单次执行取消、审计筛选/导出、计划/LDAP 作业及用例版本差异均已有页面入口。

## 本次 Actions 审计结论

2026-08-13 对主分支最新 CI 记录的复核显示：静态质量、单元/集成/性能、生产构建、部署运维和离线 SSH/systemd 安装通过；Lite 浏览器、LDAP、真实 Agent、Container、离线与 Full 验收暴露了平台配置冲突映射、状态更新默认值、页面刷新竞态、SQLite 构建初始化竞争、一次性 Runner 令牌复用及若干验收夹具偏差。本次变更逐项修正这些问题；高 CPU、浏览器、容器、真实 Agent、Full 和断网验证只在 GitHub Actions 运行。

本地只执行低资源检查：定向 Vitest、测试 TypeScript 编译、受影响包/Web 类型检查、定向 ESLint、Shell 语法和覆盖矩阵校验。远程最终结果必须绑定本次提交 SHA，不能用旧运行记录代替。

## 尚未完成

- AF-E2E-001～018 仍需按覆盖矩阵补齐真实用户/Agent/运维边界、失败和恢复组合；现有 `partial` 证据不能提前勾选。
- Gate E 必须创建指向目标提交的语义版本标签，并按[断网发布验收](./operations/offline-acceptance.md)从不可变四平台资产完成签名、SBOM、安装、真实执行、升级、故障回滚和备份恢复后才能完成。
